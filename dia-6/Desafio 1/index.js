let num = Number(prompt(`Farei a contagem de 0 até o número que você colocar:
    (Só não esquece que tem que ser inteiro e positivo, fecho?)`));

for(contador = 0; contador <= num; contador++ ){
    console.log(contador);
}